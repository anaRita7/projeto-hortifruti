import { FormValidationsPipe } from './form-validations.pipe';

describe('FormValidationsPipe', () => {
  it('create an instance', () => {
    const pipe = new FormValidationsPipe();
    expect(pipe).toBeTruthy();
  });
});
